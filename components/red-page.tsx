const RedPage = () => {
  return (
    <div
      style={{
        display: "flex",
        width: "100%",
        height: "100%",
        fontSize: "20px",
        color: "red",
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      Red Page
    </div>
  );
};

export default RedPage
